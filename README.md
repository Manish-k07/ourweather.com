# Weather Dashboard

Compatibility copy of the canonical [weather](https://github.com/Manish-k07/weather) repository.

Keep new feature work in `weather` first and synchronize this copy when needed. No weather API key is committed; the dashboard uses Open-Meteo.